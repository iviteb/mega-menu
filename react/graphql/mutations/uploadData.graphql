mutation uploadData($uploadData: [MenuInput]!) {
  uploadMenu(menuData: $uploadData) {
    id
    name
    icon
    slug
    sellerIDs
    isCollection
    styles
    display
    enableSty
    order
    slugRoot
    slugRelative
    uploadedIcon
  }
}
