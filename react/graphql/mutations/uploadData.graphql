mutation uploadData($uploadData: [MenuInput]!) {
  uploadMenu(menuData: $uploadData) {
    id
    name
    icon
    slug
    sellerIDs
    styles
    display
    enableSty
    order
    slugRoot
    slugRelative
    mobile
  }
}
